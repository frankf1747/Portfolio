{
  "name": "frank-fu-portfolio",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "//build": "with output:'export', Next treats a custom distDir as the EXPORT dir and forces the build dir back to .next -- so this puts the deployable static site in .next-build (what wrangler.jsonc serves), while build artifacts still land in .next alongside the dev server's",
    "build": "NEXT_DIST_DIR=.next-build next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@types/three": "^0.185.4",
    "lenis": "^1.1.14",
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.7",
    "sass": "^1.77.0",
    "typescript": "^5.5.0"
  }
}
